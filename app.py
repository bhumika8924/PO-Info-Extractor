"""Streamlit entrypoint.

The main Streamlit implementation lives in backend/streamlit_app.py so the
project stays separated into frontend and backend folders.
"""

from backend.streamlit_app import *  # noqa: F401,F403
